
from pathlib import Path

# Path to the main configuration file for the project.
CONFIG_FILE_PATH = Path("config/config.yaml")

# Path to the parameters file that might contain hyperparameters, settings, etc.
PARAMS_FILE_PATH = Path("params.yaml")
